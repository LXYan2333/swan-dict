--- upstream/contents/ui/Tooltip.qml
+++ current/contents/ui/Tooltip.qml
@@ -16,10 +16,32 @@
 Item {
     id: toolTipContentItem
 
-    property int preferredTextWidth: Kirigami.Units.gridUnit * 20
+    readonly property string upstreamTranslationDomain: "plasma_applet_org.kde.plasma.digitalclock"
+    function i18n(message, arg1, arg2) {
+        if (arguments.length === 3) {
+            return i18nd(upstreamTranslationDomain, message, arg1, arg2);
+        }
+        if (arguments.length === 2) {
+            return i18nd(upstreamTranslationDomain, message, arg1);
+        }
+        return i18nd(upstreamTranslationDomain, message);
+    }
+    function i18nc(context, message, arg1, arg2) {
+        if (arguments.length === 4) {
+            return i18ndc(upstreamTranslationDomain, context, message, arg1, arg2);
+        }
+        if (arguments.length === 3) {
+            return i18ndc(upstreamTranslationDomain, context, message, arg1);
+        }
+        return i18ndc(upstreamTranslationDomain, context, message);
+    }
 
-    implicitWidth: mainLayout.implicitWidth + Kirigami.Units.gridUnit
-    implicitHeight: mainLayout.implicitHeight + Kirigami.Units.gridUnit
+    property int preferredTextWidth: Kirigami.Units.gridUnit * 20
+    property int dictionaryTooltipWidth: Kirigami.Units.gridUnit * 22
+    property int dictionaryTooltipHeight: Kirigami.Units.gridUnit * 18
+    implicitWidth: root.hasDictEntry ? dictionaryTooltipWidth : mainLayout.implicitWidth + Kirigami.Units.gridUnit
+    implicitHeight: root.hasDictEntry ? dictionaryTooltipHeight : mainLayout.implicitHeight + Kirigami.Units.gridUnit
+    clip: root.hasDictEntry
 
     LayoutMirroring.enabled: Qt.application.layoutDirection === Qt.RightToLeft
     LayoutMirroring.childrenInherit: true
@@ -31,8 +53,11 @@
      * These accessible properties are used in the compact representation,
      * not here.
      */
-    Accessible.name: i18nc("@info:tooltip %1 is a localized long date", "Today is %1", tooltipSubtext.text)
+    Accessible.name: root.hasDictEntry ? (root.dictEntry.matchedWord ?? "") : i18nc("@info:tooltip %1 is a localized long date", "Today is %1", tooltipSubtext.text)
     Accessible.description: {
+        if (root.hasDictEntry) {
+            return root.dictEntry.fullText ?? "";
+        }
         const description = tooltipSubLabelText.visible ? [tooltipSubLabelText.text] : [];
         for (let i = 0; i < timeZoneRepeater.count; i += 2) {
             description.push(`${timeZoneRepeater.itemAt(i).text}: ${timeZoneRepeater.itemAt(i + 1).text}`);
@@ -51,6 +76,12 @@
 
         spacing: 0
 
+        DictionaryTooltip {
+            dictEntry: root.dictEntry
+            preferredTextWidth: toolTipContentItem.preferredTextWidth
+            visible: root.hasDictEntry
+        }
+
         Kirigami.Heading {
             id: tooltipMaintext
 
@@ -64,6 +95,7 @@
             property bool anyTimezoneSet: !!mainText
             text: anyTimezoneSet ? mainText : i18nc("@label main text shown in digital clock's tooltip when timezone is missing", "Time zone is not set")
             textFormat: Text.PlainText
+            visible: !root.hasDictEntry
         }
 
         PlasmaComponents.Label {
@@ -85,7 +117,7 @@
             }
             text: tooltipMaintext.anyTimezoneSet ? subText : i18nc("@label sub text shown in digital clock's tooltip when timezone is missing", "Click the clock icon to open Date & Time settings and set a time zone.")
             opacity: 0.6
-            visible: !clocks.visible
+            visible: !root.hasDictEntry && !clocks.visible
             font.features: { "tnum": 1 }
         }
 
@@ -96,7 +128,7 @@
             text: (root.fullRepresentationItem as CalendarView)?.monthView.todayAuxilliaryText ?? ""
             textFormat: Text.PlainText
             opacity: 0.6
-            visible: !clocks.visible && text.length > 0
+            visible: !root.hasDictEntry && !clocks.visible && text.length > 0
         }
 
         GridLayout {
@@ -105,7 +137,7 @@
             Layout.minimumWidth: Math.min(implicitWidth, toolTipContentItem.preferredTextWidth)
             Layout.maximumWidth: toolTipContentItem.preferredTextWidth
             Layout.minimumHeight: childrenRect.height
-            visible: timeZoneRepeater.count > 0 && tooltipMaintext.anyTimezoneSet
+            visible: !root.hasDictEntry && timeZoneRepeater.count > 0 && tooltipMaintext.anyTimezoneSet
             columns: 2
             rowSpacing: 0
 
